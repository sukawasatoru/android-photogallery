query ImageMeta($first: Int!, $after: String) {
  images(first: $first, after: $after) {
    __typename
    nodes {
      __typename
      url
      description
    }
    pageInfo {
      __typename
      endCursor
      hasNextPage
    }
    totalCount
  }
}
